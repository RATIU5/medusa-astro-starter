tasks:
  check:
    command: "pnpm biome check --write ."
  format:
    command: "pnpm biome format --write ."
  lint:
    command: "pnpm biome lint --write ."
  dev:
    command: "pnpm dev"
  build:
    command: "pnpm build"
  start:
    command: "pnpm start"
